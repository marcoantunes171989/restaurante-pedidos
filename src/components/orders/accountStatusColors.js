// Cor de destaque por categoria financeira — mesma semântica de
// AccountStatusBadge.jsx, em arquivo próprio (não-componente) para não
// misturar export de constante com export de componente no mesmo módulo.
export const CATEGORIA_ACCENT = { aguardando: "var(--pp-warning)", em_aberto: "var(--pp-info)", pago: "var(--pp-success)" };
